# Diabetes-Prediction-Classifier
Reproduces a machine learning experiment predicting diabetes outcomes using the PIMA Indian Diabetes dataset. Compares Logistic Regression, SVM, KNN, and Random Forest classifiers with full EDA, outlier removal via IQR, and feature importance analysis. Random Forest selected as primary classifier based on accuracy and ROC AUC performance.
